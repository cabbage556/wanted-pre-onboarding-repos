version: '3.8'
services:
  romance-service:
    build:
      context: .
      dockerfile: DockerFile
    volumes:
      - ./src:/wanted-pre-onboarding/src
    restart: always
    ports:
      - 3000:3000
    env_file:
      - ./.env
  dev-db:
    image: postgres:13
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: nest
